const ControlBar = ({}) => {
  return (
    <div>
      <button>add</button>
      <button>trash</button>
      <button>theme</button>
      <button>shortcuts</button>
    </div>
  );
};

export default ControlBar;
